# omarchy-workspace-dock
 Workspace Dock is an Omarchy bar plugin that combines workspace numbers, running applications, and a pinned application dock in one workspace-aware widget. It shows which applications are open on each workspace, lets you switch workspaces and windows, launches pinned applications, and keeps the dock positioned with the workspace controls when installed.
